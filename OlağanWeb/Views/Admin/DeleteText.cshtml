@if (ViewBag.hata != null)
{
    <h1>@ViewBag.hata</h1>
}

<form method="post">
    <div class="mb-3">
        <label for="exampleFormControlTextarea1" class="form-label">Başlık</label>
        <textarea class="form-control" required name="Title" placeholder="Başlık" id="exampleFormControlTextarea1" rows="3"></textarea>
    </div>
    <button type="submit">Sil</button>
</form>
